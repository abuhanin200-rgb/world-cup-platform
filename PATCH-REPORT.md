# Patch Report — H2H / Match Insights UI refine

## الهدف
تحسين نافذة «المواجهات والإحصائيات» في جميع أماكن استخدامها داخل المشروع، مع التركيز على:

- منع اختفاء/قصّ جزء من القائمة على الجوال.
- إبراز احتمالات الفوز بشكل أوضح.
- توضيح أي منتخب يطابق كل مؤشر/لون.
- إظهار اسم البطولة/المسابقة لكل مواجهة سابقة.
- جعل زر «المواجهات والإحصائيات» أوضح وأكثر لفتًا.
- تحسين التنسيق العام بدون تغيير بنية المشروع.

## الملفات المعدلة
- `src/components/tournaments/TournamentMatchInsights.tsx`

## أهم التحسينات
1. **رفع z-index وتحويل النافذة إلى شاشة كاملة على الجوال** لتفادي التداخل مع عناصر التنقل السفلية.
2. **توسيع المساحات السفلية (safe area)** داخل النافذة حتى لا يختفي آخر المحتوى.
3. **إعادة تصميم قسم احتمالات المباراة** مع بطاقة أفضلية بارزة وإظهار نسبة الفريق الأرجح بشكل أكبر.
4. **إعادة ضبط مؤشرات المقارنة** مع إظهار أسماء المنتخبين بجانب الألوان لتفادي انعكاس الفهم.
5. **إعادة تصميم سجل المواجهات** على شكل بطاقات أوضح مع:
   - النتيجة
   - التاريخ
   - البطولة/المناسبة
   - حالة كل منتخب في تلك المواجهة (فوز/تعادل/خسارة)
6. **تحسين زر فتح النافذة** ليكون أوضح بصريًا وأكثر بروزًا.
7. **إضافة بطاقة "آخر مواجهة"** أعلى النافذة لقراءة أسرع.

## المطلوب بعد النسخ
من داخل المشروع:

```bash
npm run typecheck
npm run build
```

إذا نجحا، ارفع:

```bash
git add .
git commit -m "Refine H2H match insights UI"
git push origin main
```
