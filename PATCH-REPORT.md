# Patch — ضبط التشكيل الحقيقي + العربية + الغيابات

## التغيير الأساسي
- إلغاء بناء تشكيل متوقع هجين من عدة مباريات.
- قبل نزول التشكيل الرسمي، يستخدم النظام أحدث تشكيل رسمي كامل للمنتخب كما هو:
  - نفس الخطة.
  - نفس الـ 11 لاعبًا.
  - نفس توزيع اللاعبين على الملعب.
  - نفس مراكز الـ grid.
- عند صدور التشكيل الرسمي للمباراة الحالية، يتحول العرض إليه تلقائيًا.

## الأسماء
- إضافة أسماء عربية موثقة للاعبي السعودية وعُمان، مع مجموعة أسماء خليجية موثقة.
- لا يتم اختراع اسم عربي عند عدم وجود اسم موثّق؛ يبقى اسم المصدر بدل تعريب آلي خاطئ.
- أسماء المدربين المعروفة تظهر بالعربية عند توفر ترجمة موثقة.

## الغيابات
- إضافة جلب غيابات المباراة من API-FOOTBALL.
- تظهر الغيابات تحت البدلاء بصورة اللاعب واسمه والسبب إذا كان متوفرًا.

## الواجهة
- الحفاظ على الملعب الأخضر 3D.
- توزيع اللاعبين يعتمد على grid الحقيقي من آخر تشكيل رسمي/التشكيل الرسمي الحالي.
- الاسم أوضح بسطرين عند الحاجة بدل قصه داخل شريط أسود.
- المركز يظهر بخط صغير أسفل الاسم.
- البدلاء والمدرب والغيابات في أقسام مستقلة.

## الملفات
- `src/data/gulfCup27ArabicNames.ts`
- `src/lib/serverApiFootball.ts`
- `src/lib/serverTournamentMatchLineup.ts`
- `src/app/api/tournaments/match-lineup/route.ts`
- `src/components/tournaments/TournamentMatchLineup.tsx`
- `src/components/GulfCup27CompetitionPanel.tsx`
- `src/components/GulfCup27PredictionsPanel.tsx`

## بعد فك الضغط
```bash
npm run typecheck
npm run build
npm run dev
```

لا يحتاج Firestore Rules ولا Firebase deploy.
