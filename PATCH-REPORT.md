# Patch — دقة المواجهات والإحصائيات

## الملفات المعدلة
- `src/lib/serverApiFootball.ts`
- `src/lib/serverTournamentMatchInsights.ts`
- `src/components/tournaments/TournamentMatchInsights.tsx`

## ما تم إصلاحه
- توحيد اتجاه Home/Away بين بيانات Firestore وAPI-FOOTBALL قبل احتساب أي نتيجة أو نسبة.
- إصلاح اسم الفائز في آخر مواجهة وسجل المواجهات.
- عكس احتمالات Home/Away ومقارنات المزود تلقائيًا إذا كانت مباراة المزود بترتيب معاكس لترتيب المنصة.
- إضافة Schema Version للكاش لإسقاط الكاش القديم الخاطئ تلقائيًا.
- عدم عرض `0%` للفورمة والهجوم والدفاع على أنه رقم حقيقي عندما تكون Comparison الخاصة بـ Predictions غير مكتملة.
- جلب آخر 5 مباريات مكتملة لكل منتخب من `/fixtures?team=...&last=5`.
- احتساب فورمة آخر 5 مباريات من النتائج الفعلية.
- عرض متوسط التسجيل، متوسط استقبال الأهداف، ونسبة الشباك النظيفة من نتائج فعلية.
- إبقاء احتمالات الفوز/التعادل كما يعيدها API-FOOTBALL بدون اختراع أو تعديل.
- إذا تساوت أعلى النسب، يظهر ترجيح تحليلي منفصل لفك التعادل، ولا يغيّر النسب الأصلية.

## استهلاك API
عند أول فتح بعد انتهاء الكاش يتم استخدام استدعاءين إضافيين فقط لجلب آخر مباريات المنتخبين.
النتيجة كاملة مخزنة في Firestore Cache، لذلك لا تتكرر الاستدعاءات لكل مستخدم.

## بعد النسخ
```bash
npm run typecheck
npm run build
```

لا يحتاج Firestore Rules ولا Firebase deploy.
