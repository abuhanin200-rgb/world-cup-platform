# Patch: إيقاف الفحص التلقائي لموسم 2026

التاريخ: 2026-09-24

## الهدف
إلغاء استدعاء API-FOOTBALL الدوري الخاص بفحص توفر موسم 2026 بعد تأكيد الموسم وربط مباريات خليجي 27، مع الإبقاء على فحص الموسم يدويًا من لوحة الإدارة.

## الملفات المعدلة
- `src/lib/serverTournamentSportsSync.ts`
- `src/components/AdminTournamentSportsApi.tsx`

## ما تغير
- الـ heartbeat / cron لم يعد يستدعي فحص الموسم.
- «مزامنة الآن» لا تعيد فحص الموسم من API؛ تعتمد على `seasonAvailability` المحفوظة.
- «اكتشاف وربط تلقائي» لا يعيد فحص الموسم إذا كان قد تم تأكيده سابقًا.
- زر «فحص موسم 2026 الآن» بقي متاحًا كفحص يدوي صريح.
- نصوص لوحة الإدارة توضح أن فحص الموسم الدوري متوقف.

## ما لم يتغير
- مزامنة حالة المباريات والنتائج المرتبطة بالـ Fixture IDs.
- احتساب النتائج التلقائي وآلية الحماية.
- الربط اليدوي Fixture ID.
- API Key وبيئة السيرفر.
- Firestore Rules.

## التحقق بعد النسخ
من جذر المشروع:

```bash
npm run typecheck
npm run build
```

لا يحتاج هذا الـ Patch إلى Firebase Rules أو `firebase deploy`.
