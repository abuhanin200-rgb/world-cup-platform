# Patch — تحسين واجهة التشكيل 3D

## التعديلات
- وضع زري «المواجهات والإحصائيات» و«التشكيل المتوقع» بجانب بعض في صف واحد داخل بطاقات المباريات والتوقعات.
- إعادة بناء أرضية التشكيل بشكل ملعب أخضر منظور/3D مع خطوط ملعب واقعية ومساحات جزاء ودائرة الوسط.
- توزيع اللاعبين داخل الملعب حسب `grid` القادم من API-FOOTBALL؛ وإذا كان غير مكتمل يتم توزيعهم حسب الخطة والمركز.
- الحارس في الأسفل، الدفاع أمامه، الوسط في المنتصف، والهجوم في أعلى الملعب.
- تصغير أسماء اللاعبين والمراكز وإضافة خلفية خفيفة للاسم لمنع تداخل النصوص.
- إبقاء صورة اللاعب ورقمه واضحة مع مسافات أفضل بين اللاعبين.
- تحسين عرض الخطة Formation أسفل الملعب.

## الملفات المعدلة فقط
- `src/components/tournaments/TournamentMatchLineup.tsx`
- `src/components/GulfCup27CompetitionPanel.tsx`
- `src/components/GulfCup27PredictionsPanel.tsx`

## بعد النسخ
```bash
npm run typecheck
npm run build
```
