# إصلاح ظهور «التشكيل غير متاح»

## السبب
كان التحقق في التشكيل المتوقع صارمًا أكثر من اللازم:
إذا لم ينجح ربط لاعب واحد فقط من التشكيل المتوقع مع Squad القادم من API-FOOTBALL، كان النظام يلغي التشكيل كاملًا ويعرض «التشكيل غير متاح».

## الإصلاح
- لا يتم إسقاط التشكيل كاملًا بسبب لاعب واحد.
- اللاعب المتوقع يُربط أولًا بالقائمة الحالية وصور/معرفات API.
- إذا تعذر ربطه، يستخدم النظام اللاعب الحقيقي الذي شغل نفس الخانة في أحدث تشكيل رسمي بدل اختراع لاعب.
- لا يتم تركيب أسماء عشوائية.
- الأسماء العربية الموثقة في التوقع تبقى عربية في الواجهة.
- رفع Schema Version للكاش إلى 7 حتى لا يرجع كاش «غير متاح» القديم.

## الملف المعدل
- `src/lib/serverTournamentMatchLineup.ts`

## بعد النسخ
```bash
npm run typecheck
npm run build
npm run dev
```

لا يحتاج Firestore Rules ولا Firebase deploy.
