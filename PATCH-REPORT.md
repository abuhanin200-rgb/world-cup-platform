# Patch — مواجهات وإحصائيات UI/UX Fix

هذا الـPatch يعدّل ملفًا واحدًا فقط:

- `src/components/tournaments/TournamentMatchInsights.tsx`

## ما تم إصلاحه

- نافذة H2H أصبحت تُفتح عبر `createPortal(document.body)` مثل قوائم «آخر التوقعات» و«جابها صح» لتخرج من أي stacking context داخل الصفحة.
- `z-index` = `9999` مع Safe Area للجوال، وBottom Sheet فعلي على الجوال وModal في الشاشات الأكبر.
- Header ثابت + محتوى داخلي مستقل بالتمرير + زر إغلاق سفلي ثابت داخل النافذة.
- منع تداخل Bottom Navigation مع قائمة المواجهات.
- توضيح آخر مواجهة: أسماء المنتخبين + النتيجة + الفائز/التعادل + البطولة + التاريخ.
- إصلاح عكس فوز/خسارة في المواجهات التاريخية؛ تحديد الفائز أصبح حسب home/away الفعليين لكل Fixture تاريخي.
- ترجمة أشهر أسماء البطولات آليًا (تصفيات كأس العالم - آسيا، كأس الخليج العربي، كأس آسيا، كأس العرب، مباراة ودية).
- الاحتمالات تظهر كسطور مستقلة وواضحة لكل منتخب والتعادل؛ لا يوجد شريط ملتبس الاتجاه.
- عند تساوي النسب، يتم ترجيح نتيجة واحدة من البيانات المتاحة باستخدام Predicted Winner ثم مؤشرات: الفورمة، الهجوم، الدفاع، Poisson، H2H، الأهداف والتقييم العام.
- النسب الأصلية من API لا يتم اختراعها أو تغييرها؛ الترجيح منفصل عنها وواضح.
- زر «المواجهات والإحصائيات» أصبح أوضح وأكثر بروزًا.

## تحقق محلي
تم تمرير الملف عبر TypeScript transpile syntax check بدون Diagnostics.

بعد فك الـZIP داخل جذر المشروع شغّل:

```bash
npm run typecheck
npm run build
```

ثم الرفع:

```bash
git add .
git commit -m "Fix H2H sheet and match insight ranking"
git push origin main
```

لا يحتاج هذا التعديل Firestore Rules أو Firebase deploy.
