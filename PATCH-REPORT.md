# تطوير لوحة حضور الأعضاء — 2026-09-24

## المنفذ

- توحيد عرض الجوال مع عرض الكمبيوتر: نفس الجدول ونفس الأعمدة، مع تمرير أفقي سلس على الشاشات الصغيرة بدل تحويل كل عضو إلى بطاقة طويلة.
- فرض التقويم الميلادي في تاريخ «آخر ظهور» على جميع الأجهزة بما فيها iPhone/Safari.
- إضافة عمود «الاتصال» عند دعم المتصفح:
  - نوع الاتصال العام مثل Wi‑Fi / شبكة جوال / Ethernet.
  - نوع الأداء التقريبي 2G/3G/4G.
  - سرعة downlink التقديرية إن توفرت.
  - حالة توفير البيانات إن توفرت.
- إضافة عمود «البطارية» عند دعم Battery Status API:
  - النسبة المئوية.
  - هل الجهاز قيد الشحن.
  - تلوين منخفض/متوسط/جيد.
  - للعضو غير المتصل تعرض آخر قراءة محفوظة.
- إضافة بطاقة إحصائية «بطارية ≤ 20%».
- البحث يشمل تفاصيل الاتصال الجديدة.
- إبقاء المتواجدين الآن أعلى القائمة وغير المتصلين تحتهم.

## حدود المتصفح

- اسم شركة الاتصالات (STC / Mobily / Zain) غير متاح مباشرة من Web APIs القياسية، لذلك لم يتم تخمينه.
- Battery Status API وNetwork Information API ليستا متاحتين في جميع المتصفحات. إذا لم يدعم جهاز العضو المعلومة، يظهر «غير متاح».
- يمكن إضافة خدمة IP/ASN خارجية مستقبلًا لتقدير مزود الإنترنت، لكنها ليست دقيقة دائمًا مع Wi‑Fi/VPN ولا تعني بالضرورة شريحة الجوال.

## الملفات

- `src/components/AdminOnlinePresencePanel.tsx`
- `src/components/PresenceTracker.tsx`
- `src/lib/presenceService.ts`
- `src/types/presence.ts`
- `firestore.rules`

## بعد النسخ

```powershell
npm run typecheck
npm run build
firebase deploy --only firestore:rules
```
