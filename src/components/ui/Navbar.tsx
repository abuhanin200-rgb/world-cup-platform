"use client";
import { useRouter } from "next/navigation";
import { useAuth } from "@/context/AuthContext";
export default function Navbar(){const router=useRouter();const{user,loading,isLoggedIn,logout}=useAuth();return <nav dir="rtl" className="sticky top-0 z-50 w-full border-b border-white/10 bg-[#061a4d]/95 backdrop-blur-xl"><div className="mx-auto flex max-w-7xl items-center justify-between gap-3 px-3 py-2.5 md:px-6"><button type="button" onClick={()=>router.push('/')} className="flex items-center gap-2"><img src="/brand/altahaddi-symbol-white.png" alt="شعار منصة التحدي" className="h-10 w-10 object-contain"/><div className="text-right"><h1 className="text-base font-black text-white">التحدي</h1><p className="text-[9px] font-bold text-white/45">توقعات · بطولات · ألعاب</p></div></button><div className="flex items-center gap-2">{loading?<span className="text-xs text-white/45">جاري التحقق…</span>:isLoggedIn&&user?<><button onClick={()=>router.push('/account')} className="min-h-[40px] rounded-xl border border-white/10 px-3 text-xs font-black">حسابي</button><button onClick={()=>{logout();router.push('/')}} className="min-h-[40px] rounded-xl bg-red-500 px-3 text-xs font-black">خروج</button></>:<button onClick={()=>router.push('/login')} className="min-h-[40px] rounded-xl bg-[var(--brand-yellow)] px-3 text-xs font-black text-[#061a4d]">دخول</button>}</div></div></nav>}
